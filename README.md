# RAG examples

here i collect a few basic examples to get started with RAG, vector databases and embedding models.


## started

```bash
python -m venv .venv
source .venv/bin/activate
pip install qdrant-client requests tqdm
```
